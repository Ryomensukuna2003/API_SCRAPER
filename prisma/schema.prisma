
generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model API_KEY {
  id        String   @id @default(cuid())
  key       String   @unique
  FoundAt   String
  api_name  String
  commit_url String
}